/**
 * Interface representing the data transfer object for user login.
 */
export interface LoginDTO {
    /**
     * The email entered by the user
     */
    email: String,
    
    /**
     * The password entered by the user
     */
    password: String
}
